Inspector = class("Inspector")

function Inspector:initialize()
	self.Entities = {}
end

function Inspector:addEntity(entity)

end

function Inspector:updateEntity(entity)

end

function Inspector:removeEntity(entity)

end